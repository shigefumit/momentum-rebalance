"""判定パラメータの一元管理。

ここの数値を変えると判定が変わる。バックテスト（Phase 1）でも同じ値を使うため、
ロジック側にマジックナンバーを書かず必ずここを参照する。
"""

# ---------------------------------------------------------------- 資金・リスク
CAPITAL_JPY = 3_000_000          # 投資資金
RISK_PER_TRADE = 0.010           # 1トレード許容損失（資金比） = 30,000円
MAX_PORTFOLIO_HEAT = 0.06        # 同時保有の合計リスク上限（絶対上限）
MAX_POSITION_PCT = 0.30          # 1銘柄への投資額上限（資金比）

# ---------------------------------------------------------------- 損切り・利確
# 研究データ: 3〜15日保有のスイングは ATR 1.5〜2.0倍が最良のリスク調整後リターン
ATR_MULT = {
    "short": 1.5,                # 短期（2〜5日）
    "swing": 2.0,                # スイング（数日〜数週間）★デフォルト
    "long": 3.0,                 # ノイズ許容型・中長期
}
STRUCT_STOP_ATR_BUFFER = 0.3     # 直近スイング安値からの下方バッファ（×ATR）
SWING_LOW_LOOKBACK = 10          # 構造ストップに使う直近安値の期間

# 損切り幅の下限（×ATR）。これがないと直近安値が偶然エントリー直下に来た時に
# 損失幅が数セントになり、値動きのノイズで即座に刈られる「機能しない損切り」が
# 生成される。同時に R:R が計算上爆発して 39:1 のような非現実的な数値になる。
# 損切り幅は必ず MIN_STOP_ATR×ATR 〜 ATR_MULT×ATR の範囲に収める。
MIN_STOP_ATR = 0.8

ENTRY_PULLBACK_ATR = 0.2         # 押し目指値 = 20EMA + 0.2×ATR
ENTRY_BREAKOUT_MARGIN = 0.001    # ブレイクアウト指値 = 前日高値 × 1.001

TP1_R = 1.0                      # 第1利確（ここで半分売却）
TP2_R = 2.5                      # 最終目標
TRAIL_LOOKBACK = 10              # トレーリング: 直近N日高値
TRAIL_ATR_MULT = 2.5             # トレーリング: 高値 − N×ATR

# ---------------------------------------------------------------- 決済方式のスイッチ
# Phase 1 の診断で「+1Rでの半分売却＋建値撤退」が勝ちを +0.5R に切り詰めていることが
# 判明したため、設計仮説を切り替えて比較できるようにしてある（validate.py で使用）。
USE_TP1_HALF = True              # +1R で半分売却し、残りの損切りを建値へ移動するか
USE_TRAILING = True              # 第1利確後にトレーリングストップを使うか
USE_SMA200_EXIT = True           # 終値が200日線を割ったら手仕舞うか

MIN_RR = 2.0                     # これ未満は「何もしない」（勝率40%でも利益が残る境界）

# ---------------------------------------------------------------- 判定しきい値
BUY_THRESHOLD = 75               # 総合スコアがこれ以上で「買い」（Phase 1で調整）

# ---------------------------------------------------------------- 指標パラメータ
SMA_LONG = 200
SMA_MID = 50
EMA_PULLBACK = 20
RSI_PERIOD = 14
RSI_FAST = 2                     # 平均回帰用
ATR_PERIOD = 14
ADX_PERIOD = 14
VOL_MA_PERIOD = 20

ADX_MIN = 22                     # もみ合い除外。これが最重要フィルター
RSI_PULLBACK_RANGE = (40, 60)    # 押し目買いの許容RSI
RSI_FAST_OVERSOLD = 10           # RSI(2) がこれ未満で売られすぎ
PULLBACK_MAX_ATR = 1.0           # 20EMAからの許容乖離（×ATR）

ATR_PCT_RANGE = (0.015, 0.06)    # ATR÷株価 の許容レンジ（低すぎ=値幅なし / 高すぎ=管理不能）
MIN_AVG_VOLUME = {"US": 500_000, "JP": 50_000}

# ---------------------------------------------------------------- 市場環境
VIX_CALM = 20                    # これ未満で平穏
VIX_CRISIS = 30                  # これ以上で危機

# ---------------------------------------------------------------- 取引コスト
COMMISSION_RATE = 0.001          # 往復手数料 0.1%
SLIPPAGE_RATE = 0.001            # スリッページ 0.1%
COST_PER_SIDE = (COMMISSION_RATE + SLIPPAGE_RATE) / 2   # 片道 0.1% / 往復 0.2%
TAX_RATE = 0.20315               # 譲渡益税 20.315%（所得税15%+復興0.315%+住民税5%）
LOSS_CARRYFORWARD_YEARS = 3      # 譲渡損失の繰越控除期間

# ---------------------------------------------------------------- バックテスト
MAX_CONCURRENT_POSITIONS = 5     # 同時保有銘柄数
ENTRY_VALID_BARS = 1             # 指値の有効期間（バー数）。約定しなければ取消
MAX_HOLD_BARS = {                # 最大保有期間。これを超えたら手仕舞い
    "short": 10,
    "swing": 60,
    "long": 250,
}

# ---------------------------------------------------------------- 市場ごとの単位
LOT_SIZE = {"JP": 100, "US": 1}  # 日本株は単元株100株、米国株は1株

# ---------------------------------------------------------------- AI層
AI_MODEL = "claude-opus-5"
AI_ADJUST_RANGE = 20             # AI補正の上限（±20点）
